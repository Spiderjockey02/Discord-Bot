module.exports.run = async (bot, ops, data, message) => {
	// This will check if the user attemptin to interact with the music plugin can in fact interact with it
	// check if DJ role is active and if the user has it
	// Could also check to make sure its in the right channel
	console.log('fdfsd');
};
